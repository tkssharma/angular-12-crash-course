import { AppMainDirective } from './app-main.directive';

describe('AppMainDirective', () => {
  it('should create an instance', () => {
    const directive = new AppMainDirective();
    expect(directive).toBeTruthy();
  });
});
